<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Readjust Defaults -->
        <item name="editTextBackground">@id/none</item>
        <item name="android:windowBackground">@color/normal_background_color</item>
    </style>

    <!-- Toolbar -->
    <style name="Widget.Toolbar" parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <item name="android:fontFamily">@font/montserrat_bold</item>
        <item name="android:textSize">@dimen/subtitle</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Bottom Menu Navigation -->
    <style name="Widget.BottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/roboto</item>
        <item name="android:textSize">@dimen/menu_tab</item>
    </style>
</resources>